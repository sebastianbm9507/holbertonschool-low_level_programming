#include "holberton.h"

/**
 * print_binary - prints the binary representation of a number
 * @n: number in base 10
 * Return: Nothing
 */
void print_binary(unsigned long int n)
{
	if (n == 0)
		return;
}
